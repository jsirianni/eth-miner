#!/bin/bash
# Managed by Chef
DEFAULT_DELAY=0
if [ "x$1" = "x" -o "x$1" = "xnone" ]; then
   DELAY=$DEFAULT_DELAY
else
   DELAY=$1
fi
sleep $DELAY
cd <%=node[:miner][:claymore_dir]%>
su <%=node[:team][:admin]%> -c "screen -dmS ethm ./mine.sh"
